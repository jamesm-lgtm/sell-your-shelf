# Staging environment credentials — copy to .env.staging and fill in.
# .env.staging is gitignored (matches .env* in .gitignore).
#
# The URL must contain the staging branch ID "dbqlgknktoctbchxfsvu" — the seed
# script refuses to run otherwise.

NEXT_PUBLIC_SUPABASE_URL=https://dbqlgknktoctbchxfsvu.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
